package stanism.marketplace.model;

/**
 * Enum representing the roles a user can have in the marketplace.
 */
public enum Role {
    /** Role for administrators with elevated privileges. */
    ADMIN,

    /** Role for regular users with standard privileges. */
    USER
}